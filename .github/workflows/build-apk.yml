name: Build APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        submodules: recursive

    - name: Install system dependencies
      run: |
        sudo apt update
        sudo apt install -y \
          python3-pip python3-setuptools python3-venv git \
          openjdk-17-jdk zip unzip build-essential autoconf automake libtool autoconf-archive pkg-config

    - name: Update git submodules
      run: git submodule update --init --recursive

    - name: Create virtual environment
      run: python3 -m venv venv

    - name: Activate venv and install Buildozer
      run: |
        source venv/bin/activate
        pip install --upgrade pip
        pip install buildozer cython setuptools

    - name: Run Buildozer once to generate platform folder
      run: |
        source venv/bin/activate
        buildozer android debug || true

    - name: Patch libffi macros
      run: |
        echo "🔧 Ищем configure.ac и патчим LT_SYS_SYMBOL_USCORE..."
        FILES=$(find .buildozer/android/platform -name "configure.ac")
        if [ -z "$FILES" ]; then
          echo "⚠️ configure.ac не найден — патч не применён"
        else
          echo "✅ Найдено $(echo "$FILES" | wc -l) файл(ов) — вносим правки..."
          echo "$FILES" | while read FILE; do
            sed -i '1im4_pattern_allow([^LT_SYS_SYMBOL_USCORE$])' "$FILE"
            echo "  🔧 Пропатчен: $FILE"
          done
          echo "🎉 Патч успешно применён"
        fi

    - name: Patch all pyjnius files (robust, Python)
      run: |
        echo "🔧 Robust patch: replacing Python2 'long' usages in pyjnius files..."
        python3 - <<'PY'
        import re, sys
        from pathlib import Path

        root = Path('.buildozer')
        if not root.exists():
            print("⚠️ .buildozer not found, skipping pyjnius patch")
            sys.exit(0)

        targets = [p for p in root.rglob('*') if ('/pyjnius/' in str(p) or '\\pyjnius\\' in str(p)) and p.suffix in ('.pxi', '.pyx', '.py')]
        if not targets:
            print("⚠️ No pyjnius .pxi/.pyx/.py files found — maybe first buildozer run didn't download them yet.")
            sys.exit(0)

        patched = []
        for p in targets:
            try:
                text = p.read_text(encoding='utf-8', errors='ignore')
            except Exception as e:
                print(f"⚠️ Could not read {p}: {e}")
                continue

            orig = text

            # 1️⃣ isinstance(arg, long) -> isinstance(arg, int)
            text = re.sub(r'isinstance\(\s*([^)]+?)\s*,\s*long\s*\)', r'isinstance(\1, int)', text)

            # 2️⃣ isinstance(arg, (int, long)) -> isinstance(arg, int)
            text = re.sub(r'isinstance\(\s*([^)]+?)\s*,\s*\(\s*int\s*,\s*long\s*\)\s*\)', r'isinstance(\1, int)', text)

            # 3️⃣ заменить словари {int: 'I', long: 'J'} → {int: 'I', int: 'J'}
            text = re.sub(r'(\b)long(\s*:\s*\'J\')', r'int\2', text)

            # 4️⃣ убрать просто "long" из tuple: (int, long) -> (int)
            text = re.sub(r'\(\s*int\s*,\s*long\s*\)', '(int)', text)

            # 5️⃣ убрать "long" из списков: [int, long] -> [int]
            text = re.sub(r'\[\s*int\s*,\s*long\s*\]', '[int]', text)

            if text != orig:
                try:
                    p.write_text(text, encoding='utf-8')
                    patched.append(str(p))
                except Exception as e:
                    print(f"⚠️ Could not write {p}: {e}")

        if patched:
            print("✅ Patched files:")
            for f in patched:
                print("  -", f)
        else:
            print("ℹ️ No files required patching.")

        print("\n🔎 Remaining 'long' occurrences inside pyjnius (first 50 results):")
        count = 0
        for p in root.rglob('*'):
            if ('/pyjnius/' in str(p) or '\\pyjnius\\' in str(p)) and p.is_file():
                try:
                    lines = p.read_text(encoding='utf-8', errors='ignore').splitlines()
                except Exception:
                    continue
                for i,l in enumerate(lines,1):
                    if 'long' in l:
                        print(f"{p}:{i}: {l.strip()}")
                        count += 1
                        break
                    if count >= 50:
                        break
        if count == 0:
            print("✅ No obvious 'long' occurrences left in pyjnius files (first-pass check).")
        PY

    - name: Build APK (second run)
      run: |
        source venv/bin/activate
        buildozer android debug

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: TestApp-APK
        path: bin/*.apk
